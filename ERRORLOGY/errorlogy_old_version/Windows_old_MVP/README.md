# Windows_old_MVP

> **STATUS: OLD SKETCH** — historical politic.bar MVP from Windows-era layout.

Main catalog: `Politic Bar (pre errorlogy)/` — methodology, seed cases, `politic_bar/` pipeline, `dashboard.html`.

Do not confuse with future production platform. See `../../ERRORLOGY_MVP_OBSIDIAN/`.
