# Windows_old_MVP

> **STATUS: OLD SKETCH** — historical politic.bar MVP from Windows-era layout.

Основной каталог: `Politic Bar (pre errorlogy)/` — методология, seed-кейсы, `politic_bar/` pipeline, `dashboard.html`.

Не путать с будущей production-платформой. См. `../../ERRORLOGY_MVP_OBSIDIAN/`.
